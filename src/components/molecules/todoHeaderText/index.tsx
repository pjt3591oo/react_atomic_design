import React from 'react';

import Font from '../../atoms/font'

const TodoHeader = (props: any) => {
  return <Font style={{fontWeight: 900, fontSize: 54}}>Todo App</Font>;
};

export default TodoHeader;
